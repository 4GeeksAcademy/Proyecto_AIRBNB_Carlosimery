import type { Room } from "@/types/room";

type RoomGalleryProps = { room: Room; activePhoto: number; onPrevious: () => void; onNext: () => void };

export const RoomGallery = ({ room, activePhoto, onPrevious, onNext }: RoomGalleryProps) => <section aria-label="Galería de fotos" className="relative mt-5 overflow-hidden rounded-3xl"><div className={`flex aspect-[1.25] min-h-[300px] items-center justify-center transition-colors duration-500 sm:aspect-[2.2] ${room.gallery[activePhoto]}`}><span className="text-7xl opacity-30">▧</span><span className="absolute bottom-4 left-4 rounded-full bg-white/90 px-3 py-1 text-xs font-semibold">Foto {activePhoto + 1} de {room.gallery.length}</span></div><button className="absolute left-4 top-1/2 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-white text-xl shadow-md transition-transform hover:scale-105" type="button" onClick={onPrevious} aria-label="Foto anterior">←</button><button className="absolute right-4 top-1/2 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-white text-xl shadow-md transition-transform hover:scale-105" type="button" onClick={onNext} aria-label="Foto siguiente">→</button></section>;
